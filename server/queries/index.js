const domain = require('./domain');
const link = require('./link');
const user = require('./user');
const visit = require('./visit');
const ip = require('./ip');

exports.default = {
    domain,
    link,
    user,
    visit,
    ip
}
